# 接手记录：MuJoCo SCORE_BLOCK 台沿修复候选

日期：2026-09-25。当前工作树未提交；本记录描述本轮实际修改与验证，供下一位代理独立检查。用户已明确要求保留现有改动并由另一个代理接手，本轮到此停止产品代码执行。

## 范围与发现

- 原报告 AC4 失败：`SCORE_BLOCK` 近台沿循环掉台、无真实 `BlockScore`。诊断程序位于 `%TEMP%/robot-simulator-edge-diag/`，曾记录车与块的位置、姿态、命令和边沿距离。早期仅将回中阈值设为 0.65 m 的版本让车在块前约 0.3 m 停推；原地掉头会让块把车夹在台沿附近。9:0 的候选比分仅来自被动分，不能当作修复成功。
- 本轮选择局部 MuJoCo FSM 控制：`src/Sim.Core/Fsm.cs` 的近沿守卫仅在目标块比车更靠近边沿且车中心边距 <0.27 m 时触发；车头朝块时沿当前朝向倒退（`V=-0.4,W=0`），否则向中心调整。对准块的原地转向限制在块离边沿 ≥0.45 m 或车距块 <0.65 m，方位误差 >0.25 rad；对准 tick 不再无条件清零无进展计时。MuJoCo 块仍在内侧时无进展限时为 8 s，靠边时为原有 2 s；legacy 限时仍为 2 s。
- 我方取得 `BlockScore` 后，若车仍在距边沿 0.65 m 内，`SEARCH` 先走 `score_retreat`：沿当前朝向离开台沿，回到 0.65 m 边距后再扫描。此分支只在 MuJoCo 启用；需检查进入/退出与异常卡住的风险。
- `src/Sim.Core/MatchEngine.cs` 的 `CoreVersion` 改为 `sim-core-1.0.2`，`src/Sim.Tests/MujocoIntegrationTests.cs` 的 MuJoCo 旧版门禁样本相应改为 `1.0.1`。`src/Sim.Tests/fixtures/restart-replay-seed42.json` 通过测试中的 `EnsureFixture()` 再生路径重录，未手工改回放内容。测试前的原文件备份于 `%TEMP%/restart-replay-seed42-before-core-1.0.2.json`。
- `src/Sim.Tests/MujocoScoreEdgeGuardTests.cs` 是进入本轮前已存在的未跟踪文件；本轮调整了单 tick 守卫断言，并增加官方 seed 42 整场测试。接手时不要把它当成可随意丢弃的临时文件。

## 当前验证记录

使用 `%TEMP%/codex-dotnet-sdk-8/dotnet.exe`（临时安装的 .NET 8 SDK）。

1. 定向测试：`dotnet test src/Sim.Tests/Sim.Tests.csproj -m:1 --filter "FullyQualifiedName~MujocoScoreEdgeGuardTests|FullyQualifiedName~MujocoIntegrationTests|FullyQualifiedName~SearchClassificationFlowTests|FullyQualifiedName~MujocoProtocolTests" --no-restore`，22/22 PASS。
2. 全套测试：`dotnet test RobotSimulator.sln -m:1 --no-restore`，373/373 PASS。首次运行因 `CoreVersion` 更新使重启回放夹具内容过期；按 `RestartRobotTests.EnsureFixture()` 的缺文件再生流程重录后通过。
3. 官方全场：`dotnet run --project src/Sim.Cli -- match --seed 42 --scenario scenarios/wushu-ring-2026-mujoco.json --events`，2400 tick，8:3，faults(us/them)=0/0。我方 `BlockScore` 在事件时间 `t=236`（+3），对手在 `t=235`（+3）；我方没有 `Drop`。新增整场测试核对了实际方块位移和出界。
4. 6 个 `replays/*.json` 旧 legacy 回放逐文件 `replay-check` 全 PASS。新 MuJoCo 回放命令：`dotnet run --project src/Sim.Cli -- replay-record --seed 42 --scenario scenarios/wushu-ring-2026-mujoco.json --out $env:TEMP/mujoco-score-edge-core-1.0.2.json`；新回放 `replay-check` 为 8:3、117/117 事件 PASS。
5. `godot --headless --path godot -- --parity-check <新回放>` 退出码 0，仅看到 `Godot Engine v4.7.2.stable.mono` 启动行，未看到明确 PASS/报告。不要把它写成已证明的 Godot parity。并行 batch 1/4 对照、32×5 s、`git diff --check` 本轮未做。

## 接手时优先核对

1. 审查 `Fsm.cs` 的 0.27/0.45/0.65 m 阈值、`score_retreat` 的退出，以及新测试是否对真实得分和无掉台有足够覆盖；抽查 `t=236` 前后接触、块位姿、得分归属。不要只看最终 8:3。
2. 补做 MuJoCo 旧 CoreVersion 回放拒绝、新回放 Godot parity 的明确结果、batch 并行确定性/32×5 s 与 `git diff --check`；据此决定 AC4/AC5 是否最终通过，并更新 `report.md` 清单。
3. 本轮开始前已有未提交修改：`.learnings/`、归档登台任务文档、`godot/src/ArenaVisualizer.cs`、`.zcode/`、`Fsm.cs` 原始候选和新增 `MujocoScoreEdgeGuardTests.cs`。本轮另修改 `MatchEngine.cs`、`MujocoIntegrationTests.cs`、重启回放夹具；不要整仓清理或覆盖别人的内容。
4. 新 RL 规划任务在 `.trellis/tasks/09-25-mujoco-score-rl-pilot/`，状态仍为 `planning`。其 `design.md` 当前写了从 `reset` 直接 `Tick(action,null)`；现有 `MatchEngine.Tick` 会把我方切到 `Manual`，因此这会训练整场而非仅 `SCORE_BLOCK`。接手代理在启动 RL 实施前应改为：`reset` 先用内置 FSM 自动推进至 `SCORE_BLOCK`，从该阶段开始策略动作，仅评测该阶段至得分/出界/我方掉台/超时；基线也按相同阶段窗口计数。训练观测的块坐标属于仿真特权信息，不能写成真机可直接部署。
