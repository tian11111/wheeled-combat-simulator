# MuJoCo 模式 SEARCH 索敌闭环

## Goal

修复 MuJoCo 模式下内置 FSM 登台成功后 SEARCH 索敌不收敛的问题:让 SEARCH 能
稳定锁定并接近目标,进入 ATTACK/推块流程,使新模式全场对抗产生真实比分。
不改变 legacy 模式行为(legacy 同 seed 比分 4:49,链路在旧模式是通的)。

## 发现证据(2026-09-25,登台修复任务 09-25-mujoco-fsm-reverse-mount/report.md)

- 双方车 ~3.2 s 登台进入 SEARCH 后,事件流反复循环:
  "对角红外发现目标[增益块] (4.0m, 左后)" → 3 s 后"目标丢失 → 继续扫描" → 再发现,
  从未进入 ATTACK/推块;60 s 全场与 32 场 batch 全部 0:0。
- 疑点①:报告的目标距离 4.0 m 超出对角红外 1.6 m 量程——疑似感知距离单位、
  目标选择来源(perception ObjectSet 真值 vs 传感器)或坐标变换不一致。
- 疑点②:即使锁定,车辆在 MuJoCo 模式的运动(惯性/打滑/转向速率)可能导致
  3 s 目标锁超时前无法收敛到攻击条件(legacy 无惯性,转向瞬时)。

## 本轮诊断结论(2026-09-25,逐 tick 轨迹 + 接触对实证)

1. **原地旋转扭矩饥饿**:指令 w=2.0 rad/s 下实际偏航仅 ~0.068 rad/s(差 30 倍)——
   原地旋转要克服四轮横向滑动摩擦(μ1.5×载荷×力臂),kv=0.25 伺服在小误差下
   只能给出 ~0.69 N·m/轮。kv=1.0/0.6 提高偏航扭矩会破坏登台(台沿冲击变硬,
   车被弹转 180°)——kv 单参数无法同时满足登台柔性与转向刚性。
2. **弧线转向的边界陷阱**:v=+0.35 弧线的移动方向沿车头——倒车登台后车头背对
   台面内部,弧线直接把车开回刚爬上来的台沿(一个前角出界 → RECOVER → 整车
   掉台)。方位取号(后目标用 −v)+ 0.5s 弧线终点预演守卫可缓解,但随后出现
   **绕块公转准稳态**:车以 ~0.3 m/s 绕块打转(自转 0.18 rad/s ≈ 公转 1.17 rad/s
   的耦合),w 恒满、方位差 ~1.3 rad 稳定不收敛,classify 无法触发。
3. **降低轮摩擦(1.5→0.9)削弱 scrub 反而破坏登台**(爬坡牵引不足)——摩擦系数
   也是单点杠杆,不能只服务转向。

## Requirements(下一轮实施建议)

- 可能的方向(需设计取舍,未定):a) FSM mujoco 分支给 SEARCH/SCORE 设计专用的
  机动序列(前进接近为主、避免绕圈,如接近-停止-微调);b) 模型层方向性抓地
  (如双contact轮胎近似);c) turn 相位的方位-速度策略离线调参(仿真批扫参)。
- 保留本轮已有成果:登台后的扫描/发现/弧线接近已能推动方块(方块位移实证),
  卡点在 classify 对准与推块决心的行为收敛。

## Acceptance Criteria

- [ ] 新模式全场(官方场景、内置 FSM)产生非零比分;legacy 逐位不变;
  全套测试与 parity 通过。

## Acceptance Criteria

- [ ] TBD:新模式全场(官方场景、内置 FSM)产生非零比分;legacy 逐位不变;
  全套测试与 parity 通过。

## Notes

- 诊断入口:`match --seed 42 --scenario scenarios/wushu-ring-2026-mujoco.json --events`
  的 SEARCH 事件循环;对照 legacy 同 seed 事件流。
- 登台修复本身见归档任务 `09-25-mujoco-fsm-reverse-mount`;本任务与登台验收任务
  `09-25-mujoco-reverse-mount-acceptance` 互不阻塞。
